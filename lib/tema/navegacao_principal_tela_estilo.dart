import 'package:flutter/material.dart';

/// Estilos específicos para a tela de Navegação Principal (BottomNavigationBar).
class NavegacaoPrincipalEstilo {
  // --- Cores ---

  /// Cor do ícone quando selecionado na barra de navegação
  static Color? corIconeSelecionado(ThemeData theme) => theme.colorScheme.primary;
}
